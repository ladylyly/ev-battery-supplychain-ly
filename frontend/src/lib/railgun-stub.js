/**
 * Railgun TypeScript File Stub
 * 
 * This is a stub module used to replace TypeScript files during webpack build.
 * The actual TypeScript files will be loaded via dynamic imports at runtime.
 * 
 * This allows the build to complete without webpack trying to process TypeScript files.
 */

export default {};
export {};

